# Copy this file to terraform.tfvars and fill in the values.
# Never commit terraform.tfvars — it contains sensitive credentials.

aws_region = "us-east-1"

environment = "mvp"

project = "backstage"

vpc_cidr = "10.0.0.0/16"

availability_zones = ["us-east-1a", "us-east-1b"]

# Leave empty to skip HTTPS on the ALB listener.
# Set to a validated ACM certificate ARN to enable HTTPS.
# e.g. "arn:aws:acm:us-east-1:123456789012:certificate/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
acm_certificate_arn = ""

# Docker image tag to deploy. Typically set by CI/CD (e.g. the Git SHA).
image_tag = "latest"

# GitHub OAuth app credentials — create at https://github.com/settings/developers
github_oauth_client_id     = "<your-github-oauth-client-id>"
github_oauth_client_secret = "<your-github-oauth-client-secret>"

# Days to retain RDS automated backups (0 disables backups)
backup_retention_days = 7

# Number of ECR images to keep; older images are pruned by lifecycle policy
image_retention_count = 10
